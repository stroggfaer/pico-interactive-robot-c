# 🤖 Robot Pico - Интерактивный Робот на Raspberry Pi Pico

## 📖 Описание проекта

**Robot Pico** - это частный образовательный проект, представляющий собой встраиваемое приложение на C/C++ для микроконтроллера Raspberry Pi Pico (RP2040). Проект создан для изучения разработки встраиваемых систем и создания интерактивного робота с эмоциональным дисплеем. Использует TFT дисплей ST7789 для отображения различных эмоций и реакций робота.

### ✨ Основные возможности

- **Эмоциональные выражения**: Нейтральное, улыбка, грусть, удивление, страх, счастье, смущение, влюбленность
- **Анимация речи**: Реалистичная анимация рта при разговоре с синхронизацией по тексту
- **USB коммуникация**: Прием команд в формате JSON через USB Serial
- **Плавные анимации**: 60 FPS анимации без мерцания с оптимизацией для встраиваемых систем
- **Автоматические переходы**: Умное переключение между эмоциями с таймерами

### 🎯 Назначение

**Образовательные цели проекта:**
- Изучение программирования микроконтроллеров RP2040 на C/C++
- Освоение работы с Raspberry Pi Pico SDK и системой сборки CMake
- Практика создания встраиваемых систем реального времени
- Изучение работы с периферией: SPI, дисплеи, USB коммуникация
- Понимание архитектуры bare-metal приложений без операционной системы
- Практика оптимизации производительности в условиях ограниченных ресурсов

## 🏗️ Архитектура системы

### Технологический стек
- **Язык**: C (C11), C++ (C++17)
- **SDK**: Raspberry Pi Pico SDK v1.5.1
- **Тулчейн**: GCC ARM 13.2 Release 1
- **Система сборки**: CMake (минимум 3.13)
- **Целевая плата**: Raspberry Pi Pico (RP2040)

### Структура проекта
```
robot_pico/
├── CMakeLists.txt              # Основная конфигурация сборки
├── pico_sdk_import.cmake       # Импорт Pico SDK
├── README.md                   # Этот файл
├── src/                        # Исходный код
│   ├── core/
│   │   ├── main.cpp           # Главный файл программы
│   │   └── states.cpp         # Управление состояниями
│   ├── display/
│   │   └── display_config.cpp # Конфигурация дисплея
│   └── emotions/
│       ├── emotions.cpp       # Логика эмоций и анимаций
│       └── mrx.cpp           # Матрицы пиксельных выражений
├── include/                    # Заголовочные файлы
│   ├── core/
│   │   ├── colors.h          # Определения цветов
│   │   └── states.h          # Структуры состояний
│   ├── display/
│   │   └── display_config.h  # Конфигурация дисплея
│   └── emotions/
│       ├── emotions.h        # Интерфейс эмоций
│       └── mrx.h            # Матрицы выражений
└── lib/                        # Внешние библиотеки
    └── st7789-library-for-pico-main/  # Драйвер дисплея ST7789
```

## 🔧 Подключение оборудования

### Подключение дисплея ST7789 (240x320)
```
Pico Pin  →  ST7789 Pin  │  Описание
─────────────────────────┼─────────────
Pin 10    →  SCK         │  SPI Clock
Pin 11    →  MOSI        │  SPI Data
Pin 9     →  CS          │  Chip Select
Pin 8     →  DC          │  Data/Command
Pin 12    →  RST         │  Reset
Pin 13    →  BL          │  Backlight
3.3V      →  VCC         │  Питание
GND       →  GND         │  Земля
```

## 🚀 Установка и сборка

### Требования к окружению

1. **Операционная система**: Windows, Linux или macOS
2. **Инструменты**:
   - CMake 3.13+
   - GCC ARM Embedded Toolchain (совместимый с версией 13.2 Rel1)
   - Raspberry Pi Pico SDK (автоматически импортируется)
   - VS Code с расширением Raspberry Pi Pico (опционально)

### Настройка среды разработки

#### Windows
```bash
# 1. Установить Raspberry Pi Pico SDK
# Скачать с https://github.com/raspberrypi/pico-sdk
# Или установить через расширение VS Code

# 2. Установить CMake
winget install Kitware.CMake

# 3. Установить ARM GCC Toolchain
# Скачать с https://developer.arm.com/tools-and-software/open-source-software/developer-tools/gnu-toolchain/gnu-rm
```

#### Linux (Ubuntu/Debian)
```bash
# 1. Установить зависимости
sudo apt update
sudo apt install cmake gcc-arm-none-eabi build-essential git

# 2. Установить Pico SDK
cd ~
git clone https://github.com/raspberrypi/pico-sdk.git --branch 1.5.1
cd pico-sdk
git submodule update --init
export PICO_SDK_PATH=~/pico-sdk
```

#### macOS
```bash
# 1. Установить Homebrew (если не установлен)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 2. Установить зависимости
brew install cmake
brew install --cask gcc-arm-embedded

# 3. Установить Pico SDK
cd ~
git clone https://github.com/raspberrypi/pico-sdk.git --branch 1.5.1
cd pico-sdk
git submodule update --init
export PICO_SDK_PATH=~/pico-sdk
```

### Сборка проекта

#### Метод 1: Командная строка
```bash
# 1. Клонировать проект (если еще не сделано)
cd /путь/к/проекту/robot_pico

# 2. Создать директорию сборки
mkdir build
cd build

# 3. Настроить сборку
cmake ..

# 4. Собрать проект
make -j4

# Результат: robot_pico.uf2 в папке build/
```

#### Метод 2: VS Code (рекомендуется)
1. Открыть папку проекта в VS Code
2. Установить расширение "Raspberry Pi Pico"
3. Нажать `Ctrl+Shift+P` → "Pico: Configure"
4. Выбрать SDK и тулчейн
5. Нажать `Ctrl+Shift+P` → "Pico: Build"

### Результаты сборки
После успешной сборки в папке `build/` будут созданы файлы:
- `robot_pico.uf2` - основной файл прошивки
- `robot_pico.elf` - исполняемый файл с отладочной информацией  
- `robot_pico.bin` - бинарный файл прошивки
- `robot_pico.hex` - Intel HEX формат

## 📱 Прошивка и запуск

### Загрузка прошивки
1. **Подготовка Pico**:
   - Отключить Pico от USB
   - Зажать кнопку BOOTSEL
   - Подключить USB (не отпуская BOOTSEL)
   - Отпустить BOOTSEL - появится диск "RPI-RP2"

2. **Прошивка**:
   ```bash
   # Метод 1: Перетаскивание файла
   # Скопировать robot_pico.uf2 на диск RPI-RP2
   
   # Метод 2: Использование picotool
   picotool load build/robot_pico.uf2 -x
   ```

### Подключение и тестирование
```bash
# Подключиться к Serial порту для мониторинга
# Windows
mode COM3 BAUD=115200 PARITY=n DATA=8

# Linux/macOS  
screen /dev/ttyACM0 115200
# или
picocom /dev/ttyACM0 -b 115200
```

## 🎮 Использование

### Формат команд JSON
Робот принимает команды в формате JSON через USB Serial:

```json
{
  "emotion": "smile",
  "duration": 5.0,
  "intensity": 0.8,
  "mouth_speed": 0.5,
  "text": "Привет мир!",
  "talking_emotion": "happy"
}
```

### Доступные эмоции
- `"neutral"` - Нейтральное выражение
- `"smile"` - Улыбка  
- `"smile_love"` - Влюбленная улыбка
- `"happy"` - Счастье
- `"sad"` - Грусть
- `"surprise"` - Удивление
- `"scary"` - Страх
- `"embarrassed"` - Смущение
- `"talking"` - Анимация речи

### Параметры команд
- `emotion` (обязательный) - название эмоции
- `duration` - длительность в секундах (по умолчанию 5.0)
- `intensity` - интенсивность от 0.0 до 1.0 (по умолчанию 0.5)
- `mouth_speed` - скорость рта от 0.1 до 2.0 (по умолчанию 0.5)
- `text` - текст для анимации речи
- `talking_emotion` - базовая эмоция для режима разговора

### Примеры команд
```bash
# Простая улыбка на 3 секунды
echo '{"emotion":"smile","duration":3.0}' > /dev/ttyACM0

# Анимация речи
echo '{"emotion":"talking","text":"Привет! Как дела?","talking_emotion":"happy","duration":10.0}' > /dev/ttyACM0

# Грустное выражение с низкой интенсивностью
echo '{"emotion":"sad","duration":5.0,"intensity":0.3}' > /dev/ttyACM0
```

## 🔧 Отладка

### Включение отладочного вывода
В файле `CMakeLists.txt` измените:
```cmake
# Включить UART отладку
pico_enable_stdio_uart(robot_pico 1)

# Или USB отладку  
pico_enable_stdio_usb(robot_pico 1)
```

### Частые проблемы

1. **Ошибки компиляции**:
   - Проверить версии SDK (1.5.1) и тулчейна (13.2)
   - Убедиться что `PICO_SDK_PATH` настроен правильно

2. **Проблемы с дисплеем**:
   - Проверить подключение SPI пинов
   - Убедиться в правильном питании (3.3V)

3. **Нет связи по USB**:
   - Проверить что включен `pico_enable_stdio_usb`
   - Использовать правильную скорость 115200

## 📊 Производительность

- **Частота анимации**: 60 FPS
- **Частота основного цикла**: 1000 Hz (1 мс)
- **Использование памяти**: ~64KB SRAM
- **Размер прошивки**: ~200KB Flash

## 🎓 О проекте

**Robot Pico** - это частный образовательный проект, созданный для изучения встраиваемых систем и программирования микроконтроллеров. Проект не предназначен для коммерческого использования и служит исключительно образовательным целям.

### Что можно изучить:
- Архитектуру RP2040 и работу с Pico SDK
- Программирование на C/C++ для встраиваемых систем
- Работу с периферией: SPI, GPIO, таймеры
- Создание плавных анимаций в условиях ограниченных ресурсов
- Протоколы коммуникации и парсинг JSON
- Системы сборки CMake для embedded проектов

## 📄 Лицензия

Это частный образовательный проект. Код предоставляется "как есть" для ознакомительных целей.

## 🆘 Поддержка

Поскольку это образовательный проект, поддержка ограничена. При возникновении проблем:
1. Проверить раздел "Частые проблемы"
2. Убедиться в правильном подключении оборудования
3. Проверить версии SDK и тулчейна
4. Изучить документацию Raspberry Pi Pico SDK

---
**Версия проекта**: 1.0  
**Последнее обновление**: 2025-09-01